import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MomentResult3Page } from './moment-result3.page';

describe('MomentResult3Page', () => {
  let component: MomentResult3Page;
  let fixture: ComponentFixture<MomentResult3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MomentResult3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
