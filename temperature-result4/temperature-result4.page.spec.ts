import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TemperatureResult4Page } from './temperature-result4.page';

describe('TemperatureResult4Page', () => {
  let component: TemperatureResult4Page;
  let fixture: ComponentFixture<TemperatureResult4Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TemperatureResult4Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
